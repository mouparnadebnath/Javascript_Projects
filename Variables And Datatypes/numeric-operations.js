// create a variable with a floating ppoint number and rounf=d it to the nearest integer
let float=3.8
let nearint=Math.round(float)
console.log(nearint)
// generate a random number
let randomnum=Math.floor(Math.random()*100)+1
console.log(randomnum)
// calculate a squaRE  and square root of a number
let a=5
let squareroot=Math.sqrt(a)
let squaRE=Math.pow(a,2)
console.log(squaRE,squareroot)
